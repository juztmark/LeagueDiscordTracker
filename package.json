{
  "name": "league-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "build/bot.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node build/bot.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "discord.js": "^12.4.1",
    "twisted": "^1.30.0"
  }
}
